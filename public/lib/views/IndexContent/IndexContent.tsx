import React, { FC } from 'react';

import { IndexDetailRouteProps, SearchMatchProps } from '../../search.types';

const IndexContent: FC<IndexDetailRouteProps<SearchMatchProps>> = () => {
	/**
	 * Methods
	 */
	/**
	 * Render
	 */

	return <p>Content</p>;
};

export default IndexContent;
